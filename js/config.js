// Replace this with your deployed Google Apps Script Web App URL (ends in /exec).
// Until then, the site falls back to the local sample data below.
var APPS_SCRIPT_URL = "";

var SAMPLE_DATA_URL = "data/schools-sample.json";
